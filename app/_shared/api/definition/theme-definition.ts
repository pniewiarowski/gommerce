interface ThemeDefinition {
    id?: number,
    title: string,
    mode: string,
    applicationTitle: string,
    primaryColor: string,
    secondaryColor: string,
    errorColor: string,
    successColor: string,
    warningColor: string,
}

export default ThemeDefinition;
